<%= form_for(@baby) do |f| %>
  <% if @baby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@baby.errors.count, "error") %> prohibited this baby from being saved:</h2>

      <ul>
      <% @baby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
    <%= f.select(:age, options_for_select([['Months', 'Months'], ['Years', 'Years']])) %>
  </div>
  <div class="field">
    <%= f.label :height %><br>
    <%= f.number_field :height %>
    <%= f.select(:height, options_for_select([['Inches', 'Inches'],['Centimeter', 'Centimeter'], ['Feet', 'Feet']])) %>
  </div>
  <div class="field">
    <%= f.label :weight %><br>
    <%= f.number_field :weight %>
    <%= f.select(:weight, options_for_select([['Lbs', 'Lbs'], ['Kgs', 'Kgs']])) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
